import React, { useState } from 'react';
import {
    View,
    Text,
    TouchableOpacity,
    StyleSheet,
    Button,
    Image,
    Modal,
    FlatList
}
    from 'react-native';
import { HeaderText, SubHeaderText } from '../Components/Styled/Text';
import { SmallButton, CheckBox, CheckMark } from '../Components/Styled/UI';
import { Colors } from '../../constants/Colors';

const CheckBoxModal = () => {
    return (
        <Modal>
            <FlatList>
                
            </FlatList>

        </Modal>
    );
};

export default CheckBoxModal;
